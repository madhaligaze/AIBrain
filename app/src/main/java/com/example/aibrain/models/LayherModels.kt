package com.example.aibrain.models

/**
 * Procedural Layher element geometry. This was unreferenced dead code (its
 * Sceneform `ShapeFactory` builders had no callers), so it's reduced to a stub
 * during the SceneView migration. Re-implement with SceneView geometry nodes if a
 * procedural (non-GLB) fallback is ever needed. See SCENEVIEW_MIGRATION.md.
 */
object LayherModels
